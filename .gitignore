# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Ignore Firebase Cache (if exists)
.firebase/

# Ignore ALL JSON files by default (Except necessary ones)
*.json
!firebase.json
!firestore.rules
!firestore.indexes.json
!storage.rules

# Ignore ALL Service Account Keys (No matter where they are)
**/serviceAccountKey.json
**/google-cloud-vision-api/*.json
**/config/google-cloud-vision-api/*.json
**/credentials/*.json

# Ignore Firebase Emulator if used
/.firebase-*
/firestore_export/